# Teleir chat rewrite foundation

این نسخه بخش چت را از حالت patchهای پراکنده به یک layout پایدارتر منتقل می‌کند.

## اصلاحات کلیدی

- تشخیص پیام خود کاربر با `id`، `email` و `name` پایدار شد تا پیام‌های کاربر همیشه سمت راست و پیام‌های طرف مقابل سمت چپ قرار بگیرند.
- ساختار ارتفاع صفحه چت در موبایل ثابت شد؛ pin، reply، edit، فایل‌های انتخاب‌شده و selection bar دیگر composer را از صفحه بیرون نمی‌برند.
- pin پیام داخل همان چت نمایش داده می‌شود و composer را جابه‌جا نمی‌کند.
- pin چت در لیست چت‌ها حفظ شده است؛ `Saved Messages` همیشه اول می‌ماند.
- فوروارد فقط وقتی دکمه Forward در نوار انتخاب یا منوی پیام زده شود باز می‌شود؛ انتخاب پیام به‌تنهایی modal باز نمی‌کند.
- فوروارد modal ارتفاع ثابت و scroll داخلی دارد.
- CSS نهایی با overrideهای تمیز در انتهای `app/globals.css` قرار گرفته تا قوانین متناقض قبلی را بی‌اثر کند.

## نکته استقرار

بعد از جایگزینی فایل‌ها:

```bash
rm -rf .next
pnpm build
pnpm start
```

اگر build خطا داد، خروجی کامل build و ۲۰۰ خط آخر لاگ زیر را ارسال کنید:

```bash
cat /var/www/teleir/storage/logs/teleir.log | tail -200
```
