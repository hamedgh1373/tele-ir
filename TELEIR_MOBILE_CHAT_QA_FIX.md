# Teleir mobile/chat UX QA fix

این نسخه بر اساس ایرادهای گزارش‌شده در موبایل و رفتارهای مشابه تلگرام اصلاح شده است.

## اصلاحات انجام‌شده

- اصلاح تب‌های لیست گفتگوها در موبایل (`All / Private / Groups / Channels / Archive`) تا به‌صورت افقی و قابل اسکرول نمایش داده شوند، نه ستون‌های بزرگ عمودی.
- اصلاح ارتفاع و اسکرول صفحه موبایل: در حالت ورود به چت، لیست پیام‌ها و لیست چت‌ها اسکرول داخلی پایدار دارند.
- جداسازی رفتار Search و More در هدر چت:
  - آیکون Search فقط نوار جستجوی داخل پیام‌ها را باز می‌کند.
  - آیکون سه‌نقطه منوی عملیات/مشخصات را باز می‌کند.
  - ناحیه پروفایل دیگر روی آیکون‌ها overlay نمی‌شود.
- ساخت گروه و کانال از طریق انتخاب مخاطبین، بدون نیاز به وارد کردن دستی ایمیل‌ها.
- API ساخت گروه/کانال علاوه بر `memberEmails`، فیلد جدید `memberIds` را هم می‌پذیرد. فیلدهای قبلی حذف نشده‌اند.
- اصلاح بهم‌ریختگی طراحی هنگام Select پیام‌ها؛ نوار انتخاب responsive و پایدار شد.
- فوروارد چندپیامه به چند مقصد هم‌زمان:
  - انتخاب چند گفتگو
  - انتخاب چند مخاطب
  - ساخت خودکار چت خصوصی برای مخاطبی که هنوز چت مستقیم ندارد
  - اسکرول داخلی لیست مقصدها
  - گزینه Hide sender name حفظ شد.
- راست‌کلیک روی چت‌ها در لیست گفتگوها:
  - Pin / Unpin chat فقط برای کاربر فعلی
  - Archive / Unarchive فقط برای کاربر فعلی
  - Mute / Unmute فقط برای کاربر فعلی
- Pin پیام‌ها نیز از این نسخه به‌صورت شخصی در `pinnedMessageIdsByUser.<userId>` ذخیره می‌شود؛ برای طرف مقابل نمایش داده نمی‌شود.
- اعلان پیام جدید پایین سمت راست صفحه اضافه شد؛ اگر صفحه باز باشد و پیام جدید از چت دیگر برسد، toast تلگرام‌مانند نمایش داده می‌شود.

## تغییرات دیتابیس افزایشی و سازگار

فیلدهای جدید اختیاری هستند و دیتابیس قبلی را خراب نمی‌کنند:

- `chats.pinnedFor: string[]`
- `chats.pinnedMessageIdsByUser: Record<string, string[]>`

فیلدهای قبلی حذف نشده‌اند:

- `pinnedMessageIds`
- `archivedFor`
- `mutedFor`
- `participantIds`
- `adminIds`

## فایل‌های تغییرکرده

- `components/chat-shell.tsx`
- `app/globals.css`
- `app/api/chats/route.ts`
- `app/api/chats/[chatId]/settings/route.ts`
- `app/api/chats/[chatId]/profile/route.ts`
- `lib/chat.ts`

## لاگ خطا

لاگ قبلی پروژه حفظ شده است:

```bash
cat /var/www/teleir/storage/logs/teleir.log | tail -200
```

یا از مرورگر با ادمین:

```text
/api/debug/logs
```

## نکته Build

در این محیط `node_modules` نصب نیست؛ بنابراین build واقعی باید روی سرور یا سیستم دارای دسترسی npm اجرا شود:

```bash
pnpm install
pnpm build
```

اگر خطا داد:

```bash
pnpm build 2>&1 | tee build.log
```

فایل `build.log` را بفرستید.
