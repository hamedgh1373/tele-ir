# TELEIR Pin Message Restored

در این نسخه گزینه Pin/Unpin پیام داخل خود چت حفظ و تقویت شد.

## تغییرات
- گزینه Pin/Unpin در منوی پیام باقی مانده است.
- اگر کاربر یک پیام را Select کند، در نوار انتخاب پایین نیز دکمه Pin/Unpin نمایش داده می‌شود.
- Pin پیام همچنان per-user است؛ یعنی فقط برای همان کاربر در همان چت نمایش داده می‌شود.
- Pin/Unpin مربوط به خود چت در لیست چت‌ها اضافه نشده است.
- دکمه کلی داخل منوی سه‌نقطه چت از `Unpin messages` به `Clear pinned messages` تغییر نام داده تا مشخص باشد همه پین‌های همان چت برای همان کاربر پاک می‌شوند.

## فایل تغییرکرده
- `components/chat-shell.tsx`
